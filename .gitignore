fonts/

# reference material only (not ours to redistribute) — fan-made maps/docs and
# other third-party assets used as data sources or design references, never
# bundled into the app itself
reference/
